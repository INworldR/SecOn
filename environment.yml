name: secon
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - numpy
  - pandas
  - matplotlib
  - seaborn
  - jupyter
  - jupyterlab
  - requests
  - python-dotenv
  - pyyaml
  - pip:
    - ruff  # Assuming this is your linter based on CHANGELOG.md
    # Other pip-only packages
